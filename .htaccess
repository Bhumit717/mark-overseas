# UNIVERSAL HTACCESS FOR ANY HOST
RewriteEngine On

# Ensure /admin routes to admin.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^admin$ admin.html [L]

# Hide sensitive files
<FilesMatch "^\.|\.json$|config\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>
